# For local development with SQLite (path is relative to prisma/schema.prisma):
DATABASE_URL="file:./dev.db"

# For production with MySQL:
# DATABASE_URL="mysql://user:password@host:port/database"

PORT=4000
JWT_SECRET="change-me-to-a-long-random-string-must-be-at-least-16-chars"
